name: Build and Push Docker Image

on:
  push:
    branches:
      - main 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v2.1.0
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Save state
      run: |
        echo "DATE=$(date +'%Y.%m.%d')" >> $GITHUB_ENV

    - name: Set repo to lowercase
      id: repo
      run: echo "::set-output name=repo::$(echo ${{ github.repository }} | awk '{print tolower($0)}')"

    - name: Build and push Docker image
      uses: docker/build-push-action@v3
      with:
        context: .
        push: true
        tags: ghcr.io/${{ steps.repo.outputs.repo }}:${{ env.DATE }}

    - name: Test if the image serves the page
      run: |
        docker run -d -p 80:80 ghcr.io/${{ steps.repo.outputs.repo }}:${{ env.DATE }}
        curl --max-time 10 --retry 5 http://localhost:80